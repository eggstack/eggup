[package]
name = "eggup-service"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
homepage = "https://github.com/eggstack/eggup"
documentation = "https://docs.rs/eggup-service"
description = "Service registration, ownership, and lifecycle adapters without automatic privilege escalation"
readme = "README.md"
authors = ["David Bowman"]
keywords = ["service", "systemd", "launchd", "lifecycle"]
categories = ["os"]
exclude = [
    "tests/",
    "benches/",
]

[lints]
workspace = true

[dependencies]
windows-args = "=0.2.0"

[target.'cfg(windows)'.dependencies]
windows-service = "=0.8.1"
